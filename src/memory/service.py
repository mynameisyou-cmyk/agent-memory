"""Core memory service: write, read, search, delete."""

from __future__ import annotations

# TODO: implement in core-build phase
# - write(project, memory_create) → MemoryCreated
# - read_by_id(project, memory_id) → MemoryOut
# - read_by_key(project, key, agent_id?) → list[MemoryOut]
# - search(project, search_params) → list[MemorySearchResult]
# - delete_by_id(project, memory_id) → MemoryDeleted
# - delete_by_key(project, key) → MemoryDeleted
