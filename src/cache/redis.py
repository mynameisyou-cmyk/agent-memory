"""Redis cache: working memory TTL store and read-through cache."""

from __future__ import annotations

# TODO: implement in core-build phase
# - get_working_memory(project_id, key) → str | None
# - set_working_memory(project_id, key, value, ttl) → None
# - cache_read(project_id, memory_id) → MemoryOut | None
# - invalidate(project_id, memory_id) → None
