"""Usage event tracking for billing."""

from __future__ import annotations

# TODO: implement in core-build phase
# - record_event(session, project_id, event_type, count=1) → None
# - get_usage(session, project_id) → UsageOut
