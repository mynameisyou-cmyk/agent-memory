"""pgvector cosine similarity search."""

from __future__ import annotations

# TODO: implement in core-build phase
# - search_similar(session, embedding, project_id, type?, limit?) → list[(Memory, score)]
